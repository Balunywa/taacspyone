apiVersion: v1
kind: Service
metadata:
    name: "akscn01-bdef"
    labels:
        app: "akscn01-bdef"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscn01-bdef"